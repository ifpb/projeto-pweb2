server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false

spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/ifpb/projeto-pweb2-config.git
          skip-ssl-validation: true
        prefix: /config